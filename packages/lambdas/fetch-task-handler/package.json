{
  "name": "fetch-task-handler",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "mkdir -p dist && npx esbuild index.ts --bundle --minify --platform=node --target=node20 --outfile=dist/index.js && cd dist && zip -r index.zip index.js && cd -"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.18",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "pg": "^8.13.3"
  }
}
